# Benchmark

This directory is reserved for later benchmark code.

The initial Scientific Data release treats the public artifact primarily as a dataset, with predefined splits suitable for benchmark-style evaluation. Full benchmark loaders, metrics, model baselines, and continual-learning protocols should be added in a later audited release.
