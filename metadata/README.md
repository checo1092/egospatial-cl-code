# Metadata

This directory will contain metadata schemas, release manifests, split files, and helper scripts used to validate the dataset release.

Expected contents before the code release DOI:

- schema documentation for episode metadata;
- release manifest validation helpers;
- split validation helpers;
- checksum verification helpers;
- public dataset repository metadata templates.

Large dataset shards must remain in the dataset repository, not in this code repository.
